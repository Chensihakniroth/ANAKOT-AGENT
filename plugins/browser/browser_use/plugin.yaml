name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed callmemo tool gateway. Also powers the 'callmemo Subscription' UX flow that bills usage to a callmemo subscription."
author: callmemo
kind: backend
provides_browser_providers:
  - browser-use
