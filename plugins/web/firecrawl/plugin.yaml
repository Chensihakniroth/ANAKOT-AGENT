name: web-firecrawl
version: 1.0.0
description: "Firecrawl web search + content extraction. Supports direct API and callmemo-hosted tool-gateway routing for subscribers. Requires FIRECRAWL_API_KEY (or FIRECRAWL_API_URL for self-hosted), or an active callmemo subscription with FIRECRAWL_GATEWAY_URL."
author: callmemo
kind: backend
provides_web_providers:
  - firecrawl
