import os
import sys

def calculate_something(x: int, y: int) -> int:
    """Calculates something important."""
    return x + y

class MyCoolClass:
    def __init__(self, name: str):
        self.name = name
    
    def greet(self):
        print(f"Hello, {self.name}!")

# Try typing "calculate_s" here to see if autocomplete works!
# Try typing "os." to see the os module suggestions!
# Try hovering over "MyCoolClass" to see hover information!
