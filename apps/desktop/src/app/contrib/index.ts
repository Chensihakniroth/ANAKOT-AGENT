// Contrib surfaces — plugin-contributed pane system.
// Wiring is the data controller + memoized pane surfaces;
// panes holds the real-data pane bodies + statusbar group setters.

export { createWiredPane, type WiredPane } from './wiring'
